server:
  port: 9998

spring:
  application:
    name: gateway
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      config:
        server-addr: 192.168.31.129:8848
        file-extension: yaml
      discovery:
        server-addr: 192.168.31.129:8848
zuul:
  routes:
    auth:
      path: /auth/**
      serviceId: auth
